{
  "name": "spotify-clone",
  "version": "1.0.0",
  "description": "Spotify clone built with Node.js/Express, Vite/Vanilla JS, and PostgreSQL",
  "main": "index.js",
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
